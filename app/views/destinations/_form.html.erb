<h1><%= @destination.id ? 'Edit Destination' : 'Create Destination' %></h1>
<hr />
<%= form_for @destination do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>
  <%= f.submit %>
<% end %>
